# MHAtomExcelTool 使用教程

---

## 目录

1. [背景](#背景)
2. [工具概述](#工具概述)
3. [主要功能](#主要功能)
   - [3.1 Excel 文件管理](#31-excel-文件管理)
   - [3.2 原子表达式编辑](#32-原子表达式编辑)
   - [3.3 动态表单系统](#33-动态表单系统)
   - [3.4 可搜索原子选择器](#34-可搜索原子选择器)
   - [3.5 代码编辑器](#35-代码编辑器)
   - [3.6 AI 智能助手](#36-ai-智能助手)
   - [3.7 原子字段配置编辑器](#37-原子字段配置编辑器)
   - [3.8 Perforce (P4V) 集成](#38-perforce-p4v-集成)
   - [3.9 全量验证检查](#39-全量验证检查)
   - [3.10 设置与主题](#310-设置与主题)
4. [常见问题](#常见问题)
5. [支持与反馈](#支持与反馈)

---

## 背景

在游戏开发过程中，策划人员需要频繁编辑和维护大量的原子配置表（Atom Excel）。这些配置表包含复杂的原子表达式，用于定义游戏中的各种逻辑，如条件判断、动作触发、数值计算等。

传统的 Excel 编辑方式存在以下痛点：

- **表达式语法复杂**：原子表达式语法规则多，手写容易出错
- **缺乏智能提示**：无法获得原子函数的参数提示和类型检查
- **调试困难**：表达式错误难以定位和排查
- **效率低下**：重复性工作多，缺乏批量处理能力

为了解决这些问题，我们开发了 **MHAtomExcelTool**，一款专为游戏策划人员设计的原子配置表可视化编辑工具。

---

## 工具概述

### 技术架构

| 技术 | 说明 |
|------|------|
| **Electron** | 跨平台桌面应用框架 |
| **Vue 3** | 响应式前端框架 |
| **TypeScript** | 类型安全的开发语言 |
| **ExcelJS** | Excel 文件读写库 |
| **Monaco Editor** | VS Code 同款代码编辑器 |
| **DaisyUI** | 美观的 UI 组件库 |

### 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  工具栏：打开文件 | 保存 | 另存为 | P4状态 | 设置           │
├───────────────┬─────────────────────────────────────────────┤
│               │  标签页：配置详情 | 原子游乐场               │
│  左侧面板     ├─────────────────────────────────────────────┤
│               │                                             │
│  搜索框       │  字段配置区域                               │
│  ────────     │  - 字段名称 + 描述                          │
│  RowName 列表 │  - 原子类型选择器                           │
│  - Row1       │  - 表达式输入框                             │
│  - Row2       │  - 动态表单编辑器                           │
│  - Row3       │                                             │
│  ...          │                                             │
│               │                                             │
├───────────────┴─────────────────────────────────────────────┤
│  底部：Sheet 标签栏 | 最近打开的文件                         │
└─────────────────────────────────────────────────────────────┘
```

### 快捷键一览

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + F` | 聚焦搜索框 |
| `Ctrl + S` | 保存文件 |
| `Ctrl + G` | 全局搜索 |
| `Ctrl + Z` | 撤销（表单内） |
| `Ctrl + Y` | 重做（表单内） |
| `Escape` | 关闭弹窗 |

---

## 主要功能

### 3.1 Excel 文件管理

#### 打开文件

**方式一：通过工具栏打开**
1. 点击工具栏的「打开文件」按钮
2. 在弹出的文件选择对话框中选择 Excel 文件
3. 支持的格式：`.xlsx`、`.xls`、`.xlsm`、`.xlsb`

**方式二：通过右键菜单打开**
1. 在 Windows 资源管理器中右键点击 Excel 文件
2. 选择「使用 MHAtomExcelTool 打开」
3. 工具将自动启动并加载文件

> 💡 **提示**：首次使用需要在工具设置中点击「加入右键菜单」注册右键菜单项

**方式三：批量打开多个文件**
1. 点击工具栏的「批量打开」按钮
2. 按住 Ctrl 键多选文件
3. 所有文件将以标签页形式打开

#### 切换工作表

- 文件加载后，底部会显示所有工作表（Sheet）标签
- 点击标签即可切换到对应工作表
- 当前工作表会高亮显示

#### 保存文件

**保存（覆盖原文件）**
- 点击工具栏「保存」按钮
- 或使用快捷键 `Ctrl + S`
- 如果文件是只读状态，工具会自动尝试解除只读后保存

**另存为**
- 点击工具栏「另存为」按钮
- 选择保存路径和文件名
- 可以保存为新文件而不影响原文件

#### 最近打开的文件

- 工具会记录最近打开的文件历史
- 点击底部的文件历史区域可快速重新打开

---

### 3.2 原子表达式编辑

原子表达式是游戏配置的核心，本工具提供了强大的表达式编辑能力。

#### 表达式语法示例

```typescript
// 数值比较
GetCombatTime() > 5

// 布尔逻辑
IsInCombat() && GetHP() < 0.5

// 复合条件
(GetLevel() >= 10 && HasBuff("PowerUp")) || IsElite()

// 带参数的函数调用
GetDistance(GetTarget()) < 100
```

#### 表达式解析流程

```
原始表达式 → 语法解析器 → JSON 对象 → 可视化表单
     ↑                                    ↓
     └──────── 表达式生成器 ←─────────────┘
```

工具会自动将文本表达式解析为结构化的 JSON 对象，并渲染为可视化表单。编辑表单后，会自动生成对应的表达式文本。

#### 原子类型体系

| 基类类型 | 说明 | 典型用途 |
|----------|------|----------|
| `NumberValueDelegate` | 数值委托 | 返回数值的计算 |
| `BoolValueDelegate` | 布尔委托 | 条件判断 |
| `ActorValueDelegate` | Actor 委托 | 获取游戏对象 |
| `EventDelegateEx` | 事件委托 | 事件监听 |
| `ActionDelegate` | 动作委托 | 执行动作 |
| `TaskDelegate` | 任务委托 | 异步任务 |

---

### 3.3 动态表单系统

动态表单是本工具的核心功能，能够根据原子元数据自动生成可编辑的 UI 表单。

#### 表单字段类型

| 类型 | 渲染方式 | 示例 |
|------|----------|------|
| `string` | 文本输入框 | 名称、描述 |
| `number` | 数字输入框 | 数值、ID |
| `boolean` | 开关 Toggle | 是否启用 |
| `select` | 下拉选择器 | 枚举值 |
| `object` | 嵌套表单 | 子原子配置 |
| `array` | 列表控件 | 多个条件/动作 |

#### 表单操作

**撤销/重做**
- `Ctrl + Z`：撤销上一步操作
- `Ctrl + Y` 或 `Ctrl + Shift + Z`：重做

**展开/折叠**
- 点击「展开全部」按钮展开所有嵌套节点
- 点击「折叠全部」按钮收起所有嵌套节点

**还原默认值**
- 每个字段旁边有 Revert 按钮（UE5 风格）
- 点击可将字段值还原为默认值

---

### 3.4 可搜索原子选择器

当需要选择原子类型时，工具提供了智能的搜索选择器。

#### 搜索功能

- **多关键词搜索**：用空格分隔多个关键词，如 `get hp` 会匹配 `GetHP`、`GetMaxHP` 等
- **搜索范围**：同时搜索函数名（funcName）和类名（className）
- **高亮显示**：匹配的关键词会高亮显示

#### 快捷输入

- **数字快捷输入**：直接输入数字（如 `100`）会自动创建 `NumberValueConstDelegate` 常量
- **分类浏览**：原子按类型分组显示，方便浏览

#### 详情预览

- 鼠标悬停在原子选项上会显示 Tooltip
- Tooltip 包含原子的详细说明、参数列表、返回类型等信息

---

### 3.5 代码编辑器

对于复杂的表达式，可以使用内置的 Monaco 代码编辑器进行编辑。

#### 功能特性

- **语法高亮**：TypeScript 语法高亮显示
- **智能提示**：自动补全原子函数名和参数
- **参数提示**：输入函数时显示参数签名
- **错误检查**：实时检查语法错误

#### 使用方式

1. 在表达式输入框右侧点击「展开」按钮
2. 弹出全屏代码编辑器
3. 编辑完成后点击「确定」保存

#### 代码预览

- 鼠标悬停在表达式输入框上会显示代码预览
- 预览窗口支持语法高亮

---

### 3.6 AI 智能助手

工具集成了 AI 大语言模型，提供智能问答服务。

#### 支持的 AI 模型

| 模型 | 特点 |
|------|------|
| 腾讯混元 Thinking | 免费使用，支持流式输出，推理能力强 |
| DeepSeek | 免费使用，响应速度快 |

#### 使用方式

1. 点击界面右下角的 AI 助手按钮
2. 在弹出的面板中输入问题
3. AI 会基于原子系统知识库回答问题

#### 典型应用场景

- **原子功能查询**：询问某个原子函数的用途和参数
- **表达式编写**：描述需求，让 AI 帮助编写表达式
- **错误排查**：粘贴错误信息，让 AI 分析原因
- **最佳实践**：询问某类配置的推荐写法

#### 快捷问题模板

面板提供了常用问题模板，点击即可快速提问：
- 「这个原子字段是做什么的？」
- 「如何编写条件判断表达式？」
- 「常用的数值计算原子有哪些？」

---

### 3.7 原子字段配置编辑器

配置编辑器用于定义哪些 Excel 字段是原子字段，以及如何识别它们。

#### 打开方式

点击右下角悬浮菜单 → 「配置编辑器」

#### 配置规则类型

**1. 后缀规则（suffixRules）**

根据字段名后缀识别原子类型：
```json
{ "value": ".Condition", "baseClass": "BoolValueDelegate" }
{ "value": ".Action", "baseClass": "ActionDelegate" }
```
例如：`Attack.Condition` 会被识别为 `BoolValueDelegate` 类型

**2. 前缀规则（prefixRules）**

根据字段名前缀识别原子类型：
```json
{ "value": "Cond_", "baseClass": "BoolValueDelegate" }
```

**3. 精确匹配规则（exactFieldNames）**

精确匹配特定字段名：
```json
{ "value": "BeginActions", "baseClass": "ActionDelegate" }
```

**4. 工作表特定规则（SpecificFieldNames）**

针对特定工作表的规则：
```json
{
  "sheetName": "技能转移逻辑原子",
  "exactFieldNames": [
    { "value": "TransferCondition", "baseClass": "BoolValueDelegate" }
  ]
}
```

#### 规则优先级

1. 工作表特定的精确匹配规则（最高优先级）
2. 全局精确匹配规则
3. 工作表特定的后缀/前缀规则
4. 全局后缀/前缀规则（最低优先级）

#### 表头行配置

某些 Excel 文件的表头行位置不在默认位置，可以单独配置：
```json
{
  "xlsxFile": "PilotMode",
  "headerRowNumber": 2,
  "dataStartRow": 4
}
```

---

### 3.8 Perforce (P4V) 集成

工具支持与 Perforce 版本控制系统集成。

#### 配置 P4 连接

1. 打开设置面板
2. 找到「P4V 配置」区域
3. 填写以下信息：
   - **P4 服务器地址**：如 `perforce:1666`
   - **P4 用户名**：您的 P4 账号
   - **P4 客户端**：您的 Workspace 名称

4. 点击「测试连接」验证配置

#### 自动 Checkout

- 当打开 P4 管理的文件时，工具会自动检测
- 如果文件未 Checkout，会弹出提示询问是否 Checkout
- 确认后自动执行 `p4 edit` 命令

#### P4 状态显示

- 工具栏右侧显示 P4 连接状态图标
- 绿色：已连接
- 灰色：未连接
- 红色：连接错误

---

### 3.9 全量验证检查

批量检查当前工作表所有原子字段的配置有效性。

#### 使用方式

1. 点击右下角悬浮菜单 → 「检查原子配置」
2. 等待检查完成
3. 查看检查结果

#### 检查内容

- 表达式语法正确性
- 原子类型是否匹配字段要求
- 必填参数是否完整
- 引用的原子是否存在

#### 结果报告

检查完成后显示：
- **总检查字段数**：本次检查的原子字段总数
- **失败字段数**：存在问题的字段数量
- **成功率**：通过检查的百分比
- **错误详情**：每个失败字段的具体错误信息

支持一键复制错误报告，方便分享和记录。

---

### 3.10 设置与主题

#### 打开设置

点击工具栏右侧的「设置」齿轮图标

#### 外观设置

**主题选择**

| 主题 | 说明 |
|------|------|
| Light | 浅色主题，适合日间使用 |
| Dark | 深色主题，护眼 |
| Dracula | 吸血鬼主题，紫色调 |
| Retro | 复古主题，怀旧风格 |
| Black | 纯黑主题，OLED 友好 |

#### 显示设置

| 设置项 | 说明 |
|--------|------|
| 只显示原子字段 | 开启后隐藏非原子字段，专注于原子配置 |
| 调试模式 | 开启后显示 JSON 解析结果，便于排查问题 |
| 字段布局方向 | 水平排列 / 垂直排列 |

#### 保存设置

| 设置项 | 说明 |
|--------|------|
| 自动保存 | 开启后定时自动保存 |
| 自动保存间隔 | 1-60 分钟可选 |

#### 更新日志

设置面板底部显示工具的版本更新日志，了解新功能和修复内容。

---

## 常见问题

### Q1: 打开 Excel 文件后看不到数据？

**可能原因：**
- 工作表中没有 RowName 列
- 表头行位置不正确

**解决方案：**
1. 确保 Excel 中有一列表头包含「RowName」字样
2. 如果表头不在第一行，在配置编辑器中设置正确的表头行位置

---

### Q2: 表达式解析失败怎么办？

**可能原因：**
- 表达式语法错误
- 使用了未定义的原子函数
- 括号不匹配

**解决方案：**
1. 检查表达式语法是否正确
2. 使用代码编辑器查看错误提示
3. 使用 AI 助手询问正确写法
4. 开启调试模式查看详细解析信息

---

### Q3: 保存文件时提示「文件只读」？

**可能原因：**
- 文件被其他程序占用
- 文件属性设置为只读
- P4 文件未 Checkout

**解决方案：**
1. 关闭其他打开该文件的程序
2. 工具会自动尝试解除只读状态
3. 如果是 P4 文件，先进行 Checkout 操作

---

### Q4: 右键菜单没有「使用 MHAtomExcelTool 打开」选项？

**解决方案：**
1. 打开工具
2. 点击右下角悬浮菜单
3. 选择「加入右键菜单」
4. 以管理员权限确认操作

---

### Q5: AI 助手无法使用？

**可能原因：**
- 网络连接问题
- AI 服务暂时不可用

**解决方案：**
1. 检查网络连接
2. 尝试切换不同的 AI 模型
3. 稍后重试

---

### Q6: 原子字段没有被正确识别？

**可能原因：**
- 字段名不符合配置的识别规则
- 配置文件未包含该字段的规则

**解决方案：**
1. 打开配置编辑器
2. 添加对应的识别规则（后缀/前缀/精确匹配）
3. 保存配置后重新加载文件

---

### Q7: 如何批量修改多行数据？

**当前限制：**
工具目前支持逐行编辑，暂不支持批量修改。

**建议：**
1. 使用全量验证检查找出所有问题行
2. 逐一修复
3. 如有批量修改需求，可反馈给开发团队

---

### Q8: 工具崩溃或卡死怎么办？

**解决方案：**
1. 查看 `logs/` 目录下的日志文件
2. 记录崩溃前的操作步骤
3. 重启工具
4. 将日志和操作步骤反馈给开发团队

---

## 支持与反馈

### 获取帮助

- **工具内 AI 助手**：点击右下角 AI 按钮，询问使用问题
- **更新日志**：设置面板查看最新功能和修复

### 反馈渠道

如果您在使用过程中遇到问题或有功能建议，请通过以下方式反馈：

1. **GitHub Issues**：在项目仓库提交 Issue，详细描述问题或建议
2. **内部沟通群**：联系开发团队成员

### 反馈内容建议

提交问题时，请尽量提供以下信息：

- **问题描述**：清晰描述遇到的问题
- **复现步骤**：如何重现这个问题
- **期望行为**：您期望的正确行为是什么
- **实际行为**：实际发生了什么
- **环境信息**：操作系统版本、工具版本
- **日志文件**：`logs/` 目录下的相关日志
- **截图**：如有必要，附上截图

### 版本信息

查看当前版本：设置面板 → 更新日志

---

**感谢使用 MHAtomExcelTool！**

如有任何问题或建议，欢迎随时反馈。我们会持续改进工具，为大家提供更好的使用体验。
